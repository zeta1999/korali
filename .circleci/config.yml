version: 2

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test

jobs:
  build:
    docker:
      - image: circleci/python:3.7.3-node

    working_directory: ~/korali

    steps:
      - checkout
      - run:
          name: run tests
          command: ./install
  test:
    macos:
      xcode: "10.2.1"

    working_directory: ~/korali

    steps:
      - checkout
      - run:
          name: run tests
          command: ./install

