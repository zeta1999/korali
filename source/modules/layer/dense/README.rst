**************************
Dense Layer
**************************

Represents a fully-connected feed-forward hidden layer in the neural network. It multiplies the inputs by a weight matrix and adds a bias to the result. Finally, it applies an activation function and passes the result to the next layer in the network. 


