

def getlibs(extdir):
 flags=''
 flags+='-L' + extdir + ' -lkorali -Wl,-rpath -Wl,' + extdir + ' '
 flags+='-L' + GSLPREFIX + '/lib -Wl,-rpath -Wl,' + GSLPREFIX + '/lib ' 
 print(flags + ' ' + GSLLIBS)
